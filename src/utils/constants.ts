export enum Role {
  Admin = 'Admin',
  SalesLeader = 'Sales Leader',
  SalesRep = 'Sales Rep',
}

export enum SortOrder {
  ASC = 'asc',
  DESC = 'desc',
}

export const tableHeader = ['Name', 'Status', 'Role', 'Last Login'];
